#include <stdlib.h>
#include "../src/datatypes.h"
#include "common.h"
#include "../src/streamer.h"
#include "../src/config.h"
#include "string.h"

#define N_PACKETS 65000

int main(void)
{
  struct opt_s* opt = malloc(sizeof(struct opt_s));

  free(opt);
  return 0;
}
